# Streamlit-Sample









# To Run Locally

Install Packages from `requirements.txt`

Run Command `streamlit run app.py`

# To run as Docker image

`docker build -t sampleapp .`

`docker run -d -p 80:80 --name=sampleapp --restart=always sampleapp`
